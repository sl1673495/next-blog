const path = require('path')

const mdDir = path.resolve(__dirname, './md')
const pageDir = path.join(__dirname, './pages')

module.exports = {
  mdDir,
  pageDir,
  owner: 'sl1673495',
  name: 'blogs',
}
